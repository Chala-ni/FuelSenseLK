# FuelSense LK — Backend (Django)

**Status:** Sprint 2 — not yet scaffolded.

Planned: Django 5 + DRF + Channels + Celery + PostGIS.

```bash
# After Sprint 2 scaffold:
docker-compose up -d
cd backend
pip install -r requirements.txt
python manage.py migrate
python manage.py runserver
```
