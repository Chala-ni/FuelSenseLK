# FuelSense LK — Web Dashboard (Flutter Web)

**Status:** Sprint 5 complete (manager dashboard + staff management).

```bash
cd web
flutter pub get
flutter run -d chrome --dart-define=API_BASE_URL=http://127.0.0.1:8000/api
```

Demo: `manager@demo.fuelsense.lk` / `DemoPass123`
