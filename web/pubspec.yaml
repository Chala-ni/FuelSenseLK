name: fuelsense_web
description: FuelSense LK — station manager & admin web dashboard
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fuelsense_ui:
    path: ../packages/fuelsense_ui
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  fl_chart: ^0.66.0
  intl: ^0.19.0
  web_socket_channel: ^2.4.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
