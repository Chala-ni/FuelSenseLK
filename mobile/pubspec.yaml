name: fuelsense_mobile
description: FuelSense LK — driver & attendant mobile app
publish_to: none
version: 0.2.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fuelsense_ui:
    path: ../packages/fuelsense_ui
  http: ^1.2.0
  flutter_secure_storage: ^9.0.0
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  web_socket_channel: ^2.4.0
  qr_flutter: ^4.1.0
  fl_chart: ^0.66.0
  intl: ^0.19.0
  geolocator: ^11.0.0
  url_launcher: ^6.2.0
  mobile_scanner: ^5.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
